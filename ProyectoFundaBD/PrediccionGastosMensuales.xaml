<Window x:Class="ProyectoFundaBD.PrediccionGastosMensuales"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gasto Mensual" Height="550" Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 12">
            <TextBlock Text="Gasto mensual con prediccion (MA3)" FontSize="20" FontWeight="Bold" Margin="0 0 12 0"/>
        </StackPanel>

        <DataGrid x:Name="dgGastoMensual"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Anno" Binding="{Binding Anio}" Width="*"/>
                <DataGridTextColumn Header="Mes" Binding="{Binding Mes}" Width="*"/>
                <DataGridTextColumn Header="Egreso" Binding="{Binding Egreso, StringFormat=N0}" Width="*"/>
                <DataGridTextColumn Header="Egreso Previo" Binding="{Binding EgresoPrev, StringFormat=N0}" Width="*"/>
                <DataGridTextColumn Header="Comportamiento (MoM) %" Binding="{Binding EgresoMoM, StringFormat=P1}" Width="*"/>
                <DataGridTextColumn Header="Prediccion (MA3)" Binding="{Binding EgresoForecastNaive, StringFormat=N0}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 12 0 0">
            <TextBlock Text="Alertas: " FontWeight="Bold" Margin="0 0 8 0"/>
            <ItemsControl x:Name="lstAlertas">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="Red"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Window>
